const axfrVerCheck = require('./axfr-version');
const spacexVerCheck = require('./spacex-version');
const rootVerCheck = require('./root-version');

module.exports = {
  axfrVerCheck,
  spacexVerCheck,
  rootVerCheck,
};

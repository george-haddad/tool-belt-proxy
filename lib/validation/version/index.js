const axfrVerCheck = require('./axfr-version');
const verifySpacexVer = require('./spacex-version');
const rootVerCheck = require('./root-version');

module.exports = {
  axfrVerCheck,
  verifySpacexVer,
  rootVerCheck,
};
